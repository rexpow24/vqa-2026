# acceptance.md — qa-pipeline V1

**Bước 02** · File này là thứ `07_verify-feature` chạy từng dòng.
Không viết ở đây = không được kiểm.

| # | Criterion | Cách kiểm | Covers |
|---|---|---|---|
| **A1** | Container chạy và **chỉ** nghe trên loopback | `docker compose up -d && docker ps` → `Up`; `docker port vlm 8080` in ra `127.0.0.1:8080`, **không** phải `0.0.0.0:8080` | AC-1, Q8 |
| **A2** | GPU thật sự được dùng **và** vision projector đã nạp | Log phải có dòng `loaded multimodal model ... mmproj-*.gguf` + dòng listening (thiếu mmproj = FAIL kể cả khi `/health` 200). GPU chứng minh bằng **thí nghiệm tắt/bật**: VRAM up→stop→up phải tụt rồi lên lại. *Sửa 2026-09-05: image này chỉ log 27 dòng và KHÔNG in tên thiết bị CUDA ở verbosity mặc định — tiêu chí cũ đòi thứ runtime không phát ra.* | AC-2, A5 |
| **A3** | VRAM nằm trong giới hạn 6 GB | `nvidia-smi --query-gpu=memory.used` ghi 3 số: trước nạp / sau nạp / lúc inference; tổng **≤ 5500 MiB** / 6144 MiB. *Sửa 2026-09-05: Windows ở chế độ WDDM trả `[N/A]` cho `--query-compute-apps`, nên không quy kết được VRAM theo process — dùng delta tổng + thí nghiệm tắt/bật của A2 thay thế.* | AC-3, A4 |
| **A4** | Inference một phát chạy được | `python -m vlm.scripts.test_api` → exit 0, in latency ms + câu trả lời | AC-4 |
| **A5** | **Model thật sự đọc ảnh** — tiêu chí trung tâm | Cùng câu hỏi, 3 điều kiện trên clip APPROVED thật: ảnh thật→A, 4 frame xám đặc→B, không ảnh→C. PASS **chỉ khi A≠B và A≠C**. Nếu A==C script exit non-zero và in ra điều kiện nào trùng | AC-5, D1, 🔴Q1 |
| **A6** | Câu trả lời khớp sự thật người đã kiểm bằng mắt | Chọn 1 clip, người xem trước và ghi expected (vd "có xe máy") vào test; assert câu trả lời chứa nó | AC-6 |
| **A7** | **Lần chạy thứ hai** ổn định | Chạy `test_api` nhiều lần, `temperature=0`: (a) hai điều kiện **có ảnh** (`real`, `grey`) phải giống hệt từng byte; (b) **verdict** `real≠grey≠blind` và phép kiểm `'vàng'` phải lặp lại được ở MỌI lần; (c) `docker ps` vẫn `Up`, restart count không tăng. *Sửa 2026-09-05 sau khi bước 07 bắt được FAIL thật: điều kiện `blind` (text-only, sinh tự do 300 token) đã lệch MỘT lần, ở đúng nhịp trạng thái prompt-cache của server thay đổi. Chạy 3 lần liên tiếp ở trạng thái ổn định thì cả ba điều kiện đều giống hệt, và verdict chưa bao giờ đổi. Đòi "byte-identical trong mọi trạng thái cache" là đòi hơn thứ llama.cpp trên CUDA hứa; thứ thật sự phải tái lập là KẾT LUẬN, và nó tái lập.* | AC-7 |
| **A8** | **Dữ liệu có chủ**: đường VLM không chạm store của pipeline | `sha256sum pipeline.db` trước và sau A4–A7 → **không đổi**. `grep -rn "INSERT\|UPDATE\|DELETE" vlm/` → 0 hit. Kết nối DB mở bằng `mode=ro` | AC-8, D2, D6 |
| **A9** | Không phá gì sẵn có | `python -m pytest tests -q` → vẫn xanh toàn bộ (baseline 55 passed) | AC-9 |
| **A10** | Lỗi có tên, không treo | Ảnh 0 byte → in tên lỗi cụ thể, exit non-zero. `docker compose down` rồi gọi → báo `connection refused` rõ ràng, không traceback trần | AC-10 |
| **A11** | Timeout không nuốt sample | Đặt timeout 1s, gọi → ghi 1 dòng JSONL `status="error"` có field `error`, exit non-zero | AC-11 |
| **A12** | Tài liệu không còn mô tả thiết kế mà code không có | `CLAUDE.md` + `architecture.md` bỏ "VQA out of scope"; `docs/01…TeamA.md` §4 và `docs/02…TeamB.md` sửa phần "VLM không bao giờ chọn đáp án" / "đừng mở đáp án gợi ý" | AC-12, conflict #4 |
| **C1** | Không hỏi nhầm cột chết | `vlm.source.approved_clips()` trả về **30 file shot thuộc 30 shot của 27 clip** đã duyệt, khớp đúng số file trên đĩa. `trimmed_path` **không xuất hiện trong bất kỳ câu SQL nào** (chỉ còn trong docstring cảnh báo về cái bẫy). *Sửa 2026-09-05, hai lý do: (a) đơn vị annotate là FILE shot chứ không phải clip — 27 clip đã duyệt sinh ra 30 file vì 3 clip được cắt làm 2 shot; (b) "0 hit khi grep" là tiêu chí sai vì nó cấm luôn cả dòng docstring giải thích tại sao không được dùng cột đó.* | conflict #1 |
| **C2** | Reject không tạo bản ghi mồ côi | Trên **bản sao** DB+file, hai đường khác nhau: (a) `review.discard()` → clip rớt khỏi danh sách (30→29) vì `discard` xoá luôn `trim_segments`; (b) file biến mất mà DB vẫn khai → clip vào mục `missing` đúng tên. Cả hai đều không để lại con trỏ hỏng im lặng. *Sửa 2026-09-05: tiêu chí cũ mô tả sai cơ chế — nó tưởng Reject sẽ đẩy clip vào `missing`, thực tế Reject dọn cả bản ghi nên `missing` chỉ dành cho trường hợp file mất ngoài luồng app. Đã chạy riêng để chứng minh nhánh `missing` không phải code chết.* | conflict #2, D6 |
| **C3** | sha256 phát hiện được clip bị cắt lại | Đổi nội dung file `trimmed/` bản sao → `source_sha256` khác lần trước → bản ghi cũ bị đánh dấu stale | conflict #2, D6 |
| **C4** | Keyframe neo theo giữa, không chia đều | `keyframe_times(29.0, 4)` → 4 mốc **đều nằm trong 3.5s quanh trung điểm 14.5s**, đối xứng, ≥1 trước và ≥1 sau. Test thuần số học, không cần ffmpeg. *Sửa 2026-09-05, hai lý do: (a) sai hệ toạ độ — D5 đọc file trong `trimmed/`, file đó ĐÃ được cắt nên mốc phải tính theo độ dài file (0..D), không phải theo offset trong clip gốc; (b) tiêu chí cũ quá yếu — với clip 10s thì chia-đều và neo-giữa cho kết quả giống nhau, phải dùng clip DÀI NHẤT thật (29.0s) mới phân biệt được: chia đều đặt frame ở 5.8s và 23.2s, cách va chạm gần 9s.* | conflict #3, D7 |
| **C5** | Không đụng footage chưa duyệt / chưa blur | `grep -rn "clips/\|delivered\|master_path" vlm/` → 0 hit tới hai thư mục đó; `approved_clips()` chỉ trả file nằm dưới `trimmed/` | conflict #5, D5 |
| **C6** | Không trùng glob của downloader | `find vlm/ work/ -name "source.*"` không sinh thêm file mới nào so với trước | conflict, `download.py:101` |
